<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :username %>
    <%= f.text_field :username %>
  </div>
    <div class="field">
    <%= f.label :price_multiplier %>
    <small>100 is no change, 105 would be 5% above the price entered.</small> <br />
    <%= f.number_field :price_multiplier %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
