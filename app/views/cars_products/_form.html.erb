<%= form_for(@cars_product) do |f| %>
  <% if @cars_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cars_product.errors.count, "error") %> prohibited this cars_product from being saved:</h2>

      <ul>
      <% @cars_product.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :car_id %><br />
    <%= f.text_field :car_id %>
  </div>
  <div class="field">
    <%= f.label :product_id %><br />
    <%= f.text_field :product_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
