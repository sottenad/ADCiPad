<%= form_for(@manufacturer) do |f| %>
  <% if @manufacturer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@manufacturer.errors.count, "error") %> prohibited this manufacturer from being saved:</h2>

      <ul>
      <% @manufacturer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name, :autofocus=>"autofocus"  %>
  </div>
  <div class="field">
	  <%= f.label :image %><br />
	  <%= f.file_field :image %><br />
	  <% if !@manufacturer.image.url(:thumb).nil? %>
	  	<%= image_tag @manufacturer.image.url(:thumb).to_s %>
	  <% end %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
